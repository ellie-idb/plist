sudo: false
language: d
d:
    - dmd
    - dmd-nightly
    - ldc
    - ldc-beta

os:
    - linux
    
install:
    - curl -L "http://bit.ly/Doveralls" | bash
    - chmod +x doveralls

script:
    - dub test --build=unittest-cov --compiler=${DC}
    - ./doveralls -travis-pro
