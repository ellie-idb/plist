sudo: false
language: d
d:
    - dmd
    - dmd-nightly
    - ldc
    - ldc-beta

os:
    - linux
    - osx
    
install:
    - wget -q -O - "http://bit.ly/Doveralls" | bash
    - chmod +x doveralls

script:
    - dub test --build=unittest-cov --compiler=${DC}
    - ./doveralls -travis-pro
